function antSuc(){
    let numero = window.prompt('Digite um número inteiro qualquer !')
    let ant = numero - 1
    let suc = 1 * numero + 1
   numero = alert(`Antes de ${numero}, temos o número ${ant}.\nDepois de ${numero}, temos o número ${suc}`);
}
